enum AuthStatus {
  initial,
  loading,
  success,
  error,
}

enum UserType {
  guest,
  free,
  premium, user,
}
enum EmotionType {
  happy,
  sad,
  angry,
  fear,
  surprise,
  disgust,
  neutral,
}

enum ChatMessageType {
  user,
  bot,
  system,
}